# Detailed Task List — cf-timetracker

**Date:** 2026-09-19
**Spec:** `2026-09-19-initial-prompt-system-architecture.md`
**Plan:** `2026-09-19-phased-plan.md`
**Usage:** Check off items in order. Each item lists files, acceptance criteria, and the exact verification command. Record `curl`/smoke transcripts in the task note.

## Phase 0 — Scaffolding & docs

- [x] **T-00 Dated docs filed** — `docs/2026-09-19-initial-prompt-system-architecture.md`, `docs/2026-09-19-brainstorm-notes.md`, `docs/2026-09-19-phased-plan.md`, `docs/2026-09-19-task-list.md` exist and match `YYYY-MM-DD-<kebab>.md`. Verify: `ls docs`.
- [x] **T-01 AGENTS.md created** — root `AGENTS.md` with layout, dated-docs rule, stack/commands, API contract, conventions, DoD. Verify: read file.
- [ ] **T-02 Layout placeholders** — `db/ worker/ web/ gas/ scripts/` exist (with `.gitkeep` until real files land). Verify: `ls`.

## Phase 1 — D1 schema, seed, toolchain

- [ ] **T-D1 `db/schema.sql`** — Port §1 schema verbatim; confirm 5 tables + 4 `idx_*` indexes. Add header comment with `wrangler d1 execute <DB_NAME> --file=./db/schema.sql` usage. Done when: fresh DB executes with no errors.
- [ ] **T-D2 `db/seed.sql`** — 2 customers, 3 projects (mixed `hourly`/`fixed`), 4 activities, 5 closed entries spanning two months + costs precomputed. IDs as UUID strings, times in epoch ms. Done when: `SELECT count(*)` matches seeded counts.
- [ ] **T-D3 Toolchain check** — `wrangler d1 execute <DB> --command="SELECT 1"` and schema/seed runs documented. Done when: commands + output pasted in task note.

## Phase 2 — Worker REST API (`worker/`)

- [ ] **T-W1 Scaffold + CORS** — `worker/package.json`, `worker/wrangler.toml` (D1 binding `DB`, compat date, `ALLOWED_ORIGINS` var), router skeleton, `OPTIONS` → 204, `jsonResponse` helper. Done when: `wrangler dev` serves `GET /api/bootstrap` (empty DB) with CORS headers. Verify: `curl -i localhost:8787/api/bootstrap`.
- [ ] **T-W2 Bootstrap + timer start/stop** — Implement §2 handlers 1–3. Validation: required `customerId/projectId/activityId`, FK existence (400 otherwise), single-timer guard (400), stop-none (404). Rate fallback: `hourlyRate ?? project.rate ?? customer.hourly_rate ?? 0`. Duration `max(1, round((end-start)/1000))`, `cost = duration/3600 * rate_applied`. Done when: `curl` cycle bootstrap → start 200 → start 400 → stop 200 → bootstrap shows finalized entry with correct math.
- [ ] **T-W3 Sheets sync with `filterMonth`** — Implement §2 handler 4 with `filterMonth: YYYY-MM` applied as UTC month slice on `start_time` (`is_running = 0` only). Invalid month → 400. GAS `fetch` with timeout, non-2xx surfaced as 502 with body excerpt. Shared secret via `GAS_SECRET` env/secret sent as header. Done when: `curl` sync with/without `filterMonth` returns `{ success: true, gasResult }`; bad month 400.
- [ ] **T-W4 Prod CORS + headers** — Env-driven `Access-Control-Allow-Origin` (allowlist, no `*` in prod), `Vary: Origin`, keep `Cf-Access-Jwt-Assertion` in allowed headers. Done when: local `*` only under dev var; prod config reviewed.

## Phase 3 — MCP server (`POST /mcp` in Worker)

- [ ] **T-M1 `tools/list`** — Return the 4 tools with stable `inputSchema`s (`get_active_timer`, `start_timer` required `customerId/projectId/activityId`, `stop_timer`, `query_summary` with `fromMs/toMs` or `month`). Done when: `curl POST /mcp '{"jsonrpc":"2.0","id":1,"method":"tools/list"}'` lists 4 tools.
- [ ] **T-M2 `tools/call` core** — `get_active_timer` (active or `{status:"idle"}`), `start_timer` (same guards as REST), `stop_timer` (same math as REST). Unknown tool → `-32601`, bad params → `-32602`. Done when: each path `curl`-tested incl. error cases.
- [ ] **T-M3 `query_summary`** — `GROUP BY` customer/project over date range; returns `[{customer, project, total_seconds, total_hours, total_cost, entry_count}]` + grand totals. Done when: seeded DB summary reconciles with `SELECT SUM(duration_seconds), SUM(cost)` spot-check.
- [ ] **T-M4 Transport decision** — Document: JSON-RPC POST suffices for v1, or spike SSE/Streamable HTTP if a named client requires it. Done when: decision + rationale appended to task note (and dated follow-up if transport changes).

## Phase 4 — Web UI (`web/` — Vite + React + Tailwind, Pages)

- [ ] **T-F1 Scaffold + `lib/api.js`** — Vite + React + Tailwind, `VITE_API_BASE` env, `api.js` wrappers (`getBootstrap`, `startTimer`, `stopTimer`, `syncSheets`) with error surfacing. Done when: `npm run dev` loads bootstrap from local Worker; `npm run build` clean.
- [ ] **T-F2 Tracker bar + live tick** — Customer/project/activity selects, description + tags inputs, Start/Stop with server-truth refresh; tick is display-only from `activeTimer.start_time`. Double-submit disabled client-side (server still guards). Done when: full cycle drivable from UI.
- [ ] **T-F3 Entries table + filters** — Last-50 table (Date, Customer, Project, Activity, Duration `HH:MM:SS`, Rate, Total), month/customer/project filters (client-side over bootstrap payload v1), totals footer. Done when: seed months filter correctly; totals match DB.
- [ ] **T-F4 CSV export** — Client-side CSV of filtered entries (same 9 columns as GAS sheet), `download="timesheet-YYYY-MM.csv"`. Done when: exported file opens with correct rows/totals.
- [ ] **T-F5 Print stylesheet** — `print.css` from §5 + screen-hidden `.print-header` (period, customer scope, totals); action bars hidden in print. Done when: print preview shows clean single-column timesheet.
- [ ] **T-F6 Sync + states** — Sync Sheets button (month-aware), refresh, loading/empty/error states for every API call. Done when: failed sync shows GAS error text, not a spinner forever.

## Phase 5 — GAS receiver (`gas/Code.gs`)

- [ ] **T-G1 Receiver + columns** — `doPost` creates `TimeEntries` with header `Entry ID, Date, Customer, Project, Activity, Duration (Hours), Cost, Description, Tags` if missing; appends mapped rows; JSON `{status,count}` / `{status:"error"}`. Done when: deployed Web App URL syncs ≥1 row from Worker.
- [ ] **T-G2 Dedupe on `Entry ID`** — Pre-read column A into a set; skip existing IDs; report `{status:"success", count, skipped}`. Done when: two identical syncs → second adds 0 rows.
- [ ] **T-G3 Shared secret** — `doPost` requires `X-Gas-Secret` (Script Property `SHARED_SECRET`) matching Worker's `GAS_SECRET`; mismatch → error JSON. Done when: bad-secret sync fails closed; secret never committed (doc setup via Script Properties + `wrangler secret`).

## Phase 6 — Zero Trust, hardening, prod deploy

- [ ] **T-Z1 Access Application** — Access app on Pages hostname; policy Allow `@yourcompany.com` or email allowlist; IdP PIN and/or Google. Done when: incognito hits Access challenge; allowed identity reaches UI.
- [ ] **T-Z2 Worker prod config** — Production `ALLOWED_ORIGINS` = Pages + custom domain only; GAS URL + secret via `wrangler secret`; D1 prod binding; note which headers (`Cf-Access-Authenticated-User-Email`, `Cf-Access-Jwt-Assertion`) are read/logged. Done when: prod bootstrap 200 from UI origin; wrong origin blocked.
- [ ] **T-Z3 Prod deploy + smoke** — `wrangler deploy` (Worker) + Pages deploy (web); custom domains if owned. Done when: prod `curl` bootstrap + start/stop + sync dry-run transcript recorded.

## Phase 7 — QA & handover

- [ ] **T-Q1 Edge-case pass** — double-click start (one 400), stop-with-none (404), month-boundary entry attribution, long-running display, CSV totals = DB `SUM()`, MCP compat re-check. Done when: each result logged pass/fail with evidence.
- [ ] **T-Q2 Follow-ups filed** — anything deferred (multi-user, invoicing, fixed-budget burn-down, SSE transport) captured in a new dated doc; no silent scope drops. Done when: doc linked here.

## Backlog (explicitly not v1)

- B1 Multi-tenancy (`user_id`, team roles). B2 Invoicing/tax. B3 Offline queue/PWA. B4 Advanced reporting dashboards. B5 Full CRUD UI for customers/projects/activities (v1: minimal create or seed+SQL-admin — decide in T-F2).
